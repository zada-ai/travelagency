-- Create database for Umrah ERP
-- Edit charset/collation if needed
CREATE DATABASE IF NOT EXISTS `umrah_erp` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Create a user and grant privileges (optional)
-- Replace 'db_user' and 'db_password' with real values before running
-- CREATE USER 'db_user'@'localhost' IDENTIFIED BY 'db_password';
-- GRANT ALL PRIVILEGES ON `umrah_erp`.* TO 'db_user'@'localhost';
-- FLUSH PRIVILEGES;
